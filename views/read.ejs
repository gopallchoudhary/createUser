<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/styles.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>

<body class="bg-zinc-900">
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <a class="text-blue-500 " href="/">Back to homepage</a>
        <h1 class="text-3xl mb-5 mt-4 bg-zinc-400 py-1 px-3 w-fit font-regular rounded-md text-blue-600 ">All Users</h1>
        <div class="users flex flex-wrap gap-3 pb-5">
            <% if (users.length> 0) { %>
                <% users.forEach(user=> { %>
                    
                        <div class="user w-[244px] p-4 bg-zinc-800 rounded-lg">
                            <div class="w-full h-52 bg-zinc-500 rounded-lg overflow-hidden">
                                <img class="w-full h-full object-cover" src="<%= user.image %>" alt="">
                            </div>
                            <h2 class="mt-3 font-medium tracking-tighter">
                                <%= user.name %>
                            </h2>
                            <h4 class="w-fit tracking-tighter text-zinc-500 text-sm">
                                <%= user.email %>
                            </h4>
                            <div class="flex gap-3 mt-4">
                                <a class="text-zinc-200" href="/edit/<%= user.id %>">Edit User</a>
                                <a class="text-red-500" href="/delete/<%= user._id %>">Delete User</a>
                            </div>
                        </div>
                    
                    
                    <% }) %>
                        <% } else { %>
                            <h3 class="text-zinc-500 font-semibold">No Users Yet</h3>

                            <% } %>







        </div>
    </div>
</body>

</html>